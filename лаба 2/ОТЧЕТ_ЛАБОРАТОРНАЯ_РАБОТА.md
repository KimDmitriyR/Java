# ОТЧЕТ ПО ЛАБОРАТОРНОЙ РАБОТЕ №2
## Тема: "Лямбда-выражения и функциональные интерфейсы в Java"

---

## ЦЕЛЬ РАБОТЫ
Изучение и практическое применение лямбда-выражений и функциональных интерфейсов в языке программирования Java для создания более компактного и читаемого кода.

---

## ЗАДАЧИ
1. Создать классы для моделирования университетской системы (преподаватели и дисциплины)
2. Реализовать функциональные интерфейсы для работы с объектами
3. Продемонстрировать различные виды лямбда-выражений
4. Показать использование ссылок на методы
5. Создать примеры блочных лямбда-выражений
6. Реализовать обобщенные функциональные интерфейсы

---

## ВЫПОЛНЕННАЯ РАБОТА

### 1. СОЗДАННЫЕ КЛАССЫ И ИНТЕРФЕЙСЫ

#### 1.1 Класс Teacher (Преподаватель)
```java
public class Teacher {
    private String fullName;           // ФИО преподавателя
    private String academicTitle;      // Ученое звание
    private String academicDegree;     // Ученая степень
    private String position;          // Должность
    private int experienceYears;      // Стаж работы
}
```

#### 1.2 Класс Discipline (Дисциплина)
```java
public class Discipline {
    private String name;              // Название дисциплины
    private String lessonType;        // Вид занятия
    private int hours;               // Количество часов
    private String semester;         // Семестр
}
```

#### 1.3 Функциональные интерфейсы
- `TeacherOperation` - для проверки условий преподавателей
- `DisciplineOperation` - для проверки условий дисциплин
- `TeacherProcessor` - для обработки преподавателей
- `DisciplineProcessor` - для обработки дисциплин
- `TeacherComparator` - для сравнения преподавателей

#### 1.4 Вспомогательный класс UniversityHelper
Содержит статические и нестатические методы для демонстрации ссылок на методы.

### 2. ДЕМОНСТРИРУЕМЫЕ ВОЗМОЖНОСТИ ЛЯМБДА-ВЫРАЖЕНИЙ

#### 2.1 Простые лямбда-выражения
```java
TeacherOperation isExperienced = teacher -> teacher.getExperienceYears() >= 5;
TeacherOperation isProfessor = teacher -> teacher.getAcademicTitle().contains("профессор");
DisciplineOperation isLecture = discipline -> discipline.getLessonType().equals("лекция");
```

#### 2.2 Лямбда-выражения как параметры методов
```java
int experiencedCount = countTeachers(teachers, t -> t.getExperienceYears() >= 10);
int lectureCount = countDisciplines(disciplines, d -> d.getLessonType().equals("лекция"));
```

#### 2.3 Ссылки на методы
```java
// Ссылки на статические методы
int experiencedCount = countTeachers(teachers, UniversityHelper::isExperiencedTeacher);

// Ссылки на нестатические методы
UniversityHelper helper = new UniversityHelper();
int phdCount = countTeachers(teachers, helper::hasPhD);
```

#### 2.4 Лямбда-выражения как результат методов
```java
private TeacherProcessor getTeacherProcessor(int type) {
    switch (type) {
        case 1: return teacher -> "Краткая информация: " + teacher.getFullName();
        case 2: return teacher -> "Подробная информация: " + teacher.toString();
        case 3: return teacher -> "Статус: " + (teacher.getExperienceYears() >= 10 ? "Опытный" : "Начинающий");
        default: return teacher -> "Неизвестный тип обработки";
    }
}
```

#### 2.5 Блочные лямбда-выражения
```java
TeacherProcessor complexProcessor = teacher -> {
    StringBuilder result = new StringBuilder();
    result.append("=== ДЕТАЛЬНАЯ ИНФОРМАЦИЯ ===\n");
    result.append("ФИО: ").append(teacher.getFullName()).append("\n");
    result.append("Должность: ").append(teacher.getPosition()).append("\n");
    result.append("Стаж: ").append(teacher.getExperienceYears()).append(" лет\n");
    
    if (teacher.getExperienceYears() >= 10) {
        result.append("Статус: ОПЫТНЫЙ ПРЕПОДАВАТЕЛЬ\n");
    } else {
        result.append("Статус: МОЛОДОЙ ПРЕПОДАВАТЕЛЬ\n");
    }
    
    return result.toString();
};
```

#### 2.6 Обобщенные функциональные интерфейсы
```java
TeacherComparator byExperience = (t1, t2) -> Integer.compare(t1.getExperienceYears(), t2.getExperienceYears());
TeacherComparator byName = (t1, t2) -> t1.getFullName().compareTo(t2.getFullName());
```

### 3. ПРОДЕМОНСТРИРОВАННЫЕ ПРИМЕРЫ

#### 3.1 Создание тестовых данных
- 4 преподавателя с различными характеристиками
- 4 дисциплины разных типов и продолжительности

#### 3.2 Выполняемые операции
- Подсчет преподавателей по различным критериям
- Подсчет дисциплин по типу и продолжительности
- Обработка и форматирование информации
- Сравнение объектов по различным параметрам

---

## РЕЗУЛЬТАТЫ РАБОТЫ

### Достигнутые цели:
1. ✅ Создана полноценная модель университетской системы
2. ✅ Реализованы все необходимые функциональные интерфейсы
3. ✅ Продемонстрированы все основные виды лямбда-выражений
4. ✅ Показано использование ссылок на методы
5. ✅ Созданы примеры блочных лямбда-выражений
6. ✅ Реализованы обобщенные функциональные интерфейсы

### Особенности реализации:
- Использование аннотации `@FunctionalInterface` для явного указания функциональных интерфейсов
- Комбинирование различных подходов к использованию лямбда-выражений
- Создание читаемого и понятного кода с хорошей структурой
- Демонстрация практических применений в реальных задачах

---

## ВЫВОДЫ

1. **Лямбда-выражения** значительно упрощают код и делают его более читаемым
2. **Функциональные интерфейсы** позволяют создавать гибкие и переиспользуемые компоненты
3. **Ссылки на методы** обеспечивают еще большую компактность кода
4. **Блочные лямбда-выражения** позволяют реализовывать сложную логику
5. **Обобщенные интерфейсы** повышают типобезопасность и гибкость кода

Лямбда-выражения являются мощным инструментом современного Java, который позволяет писать более функциональный и элегантный код, особенно при работе с коллекциями и обработке данных.

---

## ТЕХНИЧЕСКИЕ ДЕТАЛИ

- **Язык программирования**: Java
- **Используемые возможности**: Java 8+ (лямбда-выражения, функциональные интерфейсы)
- **Архитектура**: Объектно-ориентированное программирование с элементами функционального программирования
- **Структура проекта**: Пакет `university` с логически разделенными классами

---

*Отчет подготовлен по результатам выполнения лабораторной работы №2*
